<%= form_with(model: slot, local: true) do |form| %>
  <% if slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slot.errors.count, "error") %> prohibited this slot from being saved:</h2>

      <ul>
      <% slot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic, id: :slot_topic %>
  </div>

  <div class="field">
    <%= form.label :speaker %>
    <%= form.text_field :speaker, id: :slot_speaker %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :slot_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
